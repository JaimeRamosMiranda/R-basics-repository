# Creating a sequence of number that starts in 1 until the assigned value of n
n <- 1000
x <- seq(1,n)
sum(x)

# log to the base 2
log2(16)
[1] 4
 
# sqrt of the log to the base 2 of 16:
sqrt(log2(16))
[1] 2

# Compute log to the base 10 (log10) of the sqrt of 100. Do not use variables.
log10(sqrt(100))
[1] 1


